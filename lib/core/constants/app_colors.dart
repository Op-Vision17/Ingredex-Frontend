import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Brand colors from palette
  static const primaryOrange = Color(0xFFFBAB57);
  static const lightOrange = Color(0xFFFEC674);
  static const darkBlack = Color(0xFF222222);
  static const creamWhite = Color(0xFFFFF3E2);

  // Light theme colors
  static const lightBackground = Color(0xFFFFF3E2);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightText = Color(0xFF222222);
  static const lightTextSecondary = Color(0xFF666666);
  static const lightDivider = Color(0xFFE8E0D5);

  // Dark theme colors
  static const darkBackground = Color(0xFF1A1A1A);
  static const darkSurface = Color(0xFF2A2A2A);
  static const darkCard = Color(0xFF2A2A2A);
  static const darkText = Color(0xFFFFF3E2);
  static const darkTextSecondary = Color(0xFFAAAAAA);
  static const darkDivider = Color(0xFF3A3A3A);

  // Semantic colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFF9800);
  static const error = Color(0xFFF44336);
  static const lowRisk = Color(0xFF4CAF50);
  static const mediumRisk = Color(0xFFFF9800);
  static const highRisk = Color(0xFFF44336);
}
